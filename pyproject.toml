[project]
name = "lms-polinema-mcp"
version = "2.0.0"
description = "MCP server for LMS Polinema — reference implementation for campus LMS integration with AI agents"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "Hafidz Rafi Rabbani", email = "mohammadhafidzrafirabbani99@gmail.com" }
]
requires-python = ">=3.11"
keywords = ["mcp", "lms", "moodle", "ai-agent", "campus", "education", "polinema"]

[project.urls]
Homepage = "https://github.com/hafidzrafi/lms-polinema-mcp"
Repository = "https://github.com/hafidzrafi/lms-polinema-mcp"
Issues = "https://github.com/hafidzrafi/lms-polinema-mcp/issues"
Changelog = "https://github.com/hafidzrafi/lms-polinema-mcp/blob/main/CHANGELOG.md"

classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "beautifulsoup4>=4.12.0",
    "httpx>=0.28.0",
    "lxml>=5.0.0",
    "mcp>=2.2.0",
    "playwright>=1.40.0",
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "respx>=0.21.0",
    "ruff>=0.4.0",
]

[project.scripts]
lms-polinema-mcp = "lms_polinema_mcp.server:main"
lms-polinema-auth = "lms_polinema_mcp.auth.credentials:main"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.uv]
package = true

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
